version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./backend/data:/app/data
    environment:
      - PORT=3001
      - REFRESH_INTERVAL=*/5 * * * *
      - DATA_DIR=/app/data
    restart: unless-stopped
