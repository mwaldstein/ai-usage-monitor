PORT=3001
REFRESH_INTERVAL=*/5 * * * *
# DATA_DIR=/path/to/data  # Optional: explicit data directory path
# AUTH_SECRET=change-me-to-a-long-random-string  # Optional: set for stable session signing (auto-generated if omitted, min 16 chars)
